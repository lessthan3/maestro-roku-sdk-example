{
    "version": "2.0.0",
    "tasks": [
        {
          "label": "Launch App",
          "command": "npx",
          "args": ["bsc", "--project", "bsconfig.json", "--stagingDir", "build"]
        }
    ]
}
